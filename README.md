# TL Smoke Test: Mini Weather Badge

A simple HTML/CSS/JS weather badge.

## Features

- City name, temperature, weather icon and condition label
- Click the badge to cycle through random weather conditions

## Files

- `index.html` — markup
- `style.css` — styling
- `index.js` — interaction logic

## Test

```bash
node --check index.js
```
